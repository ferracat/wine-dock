ARG BASE_IMAGE=wine-base
FROM ${BASE_IMAGE}

# User = the one defined in the base image (make image USER_NAME=...)
# Optional: pre-initialize prefix in the image (faster first run)
# RUN xvfb-run -a wineboot --init && wineserver -w
